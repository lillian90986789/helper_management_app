# Caddy 反向代理 + 自动 HTTPS
# 把下面的 your-domain.com 换成你自己的域名（需先把域名 A 记录指向服务器 IP）。
# Caddy 会自动向 Let's Encrypt 申请并续期证书，你什么都不用管。

helpermanagement.xyz {
	reverse_proxy homeflow:8080
}

# 如果你暂时没有域名，只想用 IP 测试：
#   不要用这个 Caddyfile，直接用原来的 docker-compose.yml 起服务，
#   然后访问 http://你的服务器IP:8080 即可（无 HTTPS，仅供测试）。
